{
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "name": "splyt",
  "scripts": {
      "start": "lerna run --parallel start",
      "test": "lerna run --parallel test",
      "bootstrap": "lerna bootstrap"
  }
}
